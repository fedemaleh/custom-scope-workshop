# Custom Scope Workshop

You can store your backup manifests in the `manifests` applications. You should create a subfolder structure $application_id/$scope_id, and put all your manifests inside that folder.
